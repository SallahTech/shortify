{
  "name": "shortify-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run start:dev --workspace=backend",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=frontend & npm run start:prod --workspace=backend",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
